/**
 * Interface for a task display model.
 */
export interface ITaskDisplayModel {
  /**
   * The task's internal id url.
   */
  taskId: string;
  /**
   * The task's header.
   */
  header: string;
}
